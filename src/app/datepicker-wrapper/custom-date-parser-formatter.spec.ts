import { CustomDateParserFormatter } from './custom-date-parser-formatter';

describe('CustomDateParserFormatter', () => {
  it('should create an instance', () => {
    expect(new CustomDateParserFormatter()).toBeTruthy();
  });
});
